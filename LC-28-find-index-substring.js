// LC-28


class Main {
  constructor(){
  }

  findSubstringBruteforce(bigStr, subStr){
    let count = 0;
    let index = 0;
    const subLength = subStr.length

    for(let i = 0; i < bigStr.length; i++){
      if(count === l)
      if(bigStr[i] == subStr)
    }
  }
}